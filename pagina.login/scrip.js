const carrito = document.querySelector(".carrito");

carrito.addEventListener("click", () => {
    carrito.classList.toggle("abierto");
});


li.textContent = `${nombre} - $${precio.toLocaleString()} COP`;

totalSpan.textContent = total.toLocaleString();
